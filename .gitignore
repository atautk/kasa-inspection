# Python
__pycache__/
*.pyc

# VS Code
.vscode/

# Sanal ortam
venv/
.venv/
.env/

# Geçici dosyalar
*.log

# Inspection log veritabanları (çalışma zamanı verisi, banda özel)
inspection_log.db

# NG kareleri (çalışma zamanı verisi, banda özel)
ng_captures/

# Tanınmayan kasa fotoğrafları (çalışma zamanı verisi, banda özel)
unknown_kasa_captures/

# Pencere boyutu/konum tercihleri (makineye özel)
window_settings.ini

# Operatör listesi ve PIN hash'leri (makineye özel, PIN içerdiği için git'e girmemeli)
configuration/operators.json

# Telegram bot token'ı (kimlik bilgisi, git'e girmemeli)
configuration/telegram_settings.json

# Telegram bildirim alıcıları (telefon numarası/chat id, kişisel bilgi)
configuration/telegram_recipients.json

# Gönderilemeyen Telegram bildirimlerinin kuyruğu (çalışma zamanı
# verisi, bot token içerdiği için git'e girmemeli)
configuration/telegram_queue.json

# Periyodik özet rapor dosyaları (çalışma zamanı verisi, banda özel)
telegram_reports/

# Model eğitimi için biriktirilen ROI görüntü çiftleri (çalışma
# zamanı verisi, banda özel, potansiyel olarak büyük)
training_data/

# Kamera kalibrasyon çıktıları (istersen bunları sonradan ekleyebilirsin)
# calibration/*.npy

# Deneme amaçlı oluşturulan test bandları (gerçek üretim
# konfigürasyonu değil, git'e girmesin)
configuration/band_02/
configuration/band_03/

# band_01'e test amaçlı eklenen ek kamera kanalı verisi (deneme,
# gerçek üretim konfigürasyonu değil)
configuration/band_01/cameras/

# band_01/band.json makineye özel test ayarlarıyla (Arduino portu,
# ek kamera kanalı) değiştiriliyor - takipten çıkarıldı, git rm
# --cached
configuration/band_01/band.json